<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="service.iptvhelperfixes" name="IPTV Helper Fixes" version="1.0.2" provider-name="ahmadashwah">
    <requires>
        <import addon="xbmc.python" version="3.0.0"/>
    </requires>
    <extension point="xbmc.service" library="service.py" start="startup" />
    <extension point="xbmc.addon.metadata">
        <summary lang="en_GB">Auto-applies XStream Player + TMDb Bingie Helper fixes</summary>
        <description lang="en_GB">Runs once at Kodi startup. Wires TMDb Bingie Helper's player config to XStream Player, patches known XStream Player bugs (series ID lookup, provider name-prefix cleanup, EPG notification spam), and sets XStream Player as the default player so Discover/search play directly without a chooser dialog. Safe to re-run — checks before changing anything, and will not touch a different XStream Player version than the one these patches were built against, to avoid corrupting it.</description>
        <platform>all</platform>
        <license>MIT</license>
        <source>https://github.com/ahmadashwah/BenjiePatches</source>
        <news>1.0.2 - Fixed a crash on Android when backing up/replacing addon.py (shutil.copy2's metadata copy is refused by Android's storage layer; switched to a plain content copy)
1.0.1 - If the fix-check itself crashes, it now shows the full error directly on screen (no log file/uploader needed)
1.0.0 - Initial release</news>
    </extension>
</addon>
